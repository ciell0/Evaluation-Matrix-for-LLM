metrics:
  - bertscore
  - format_validity
  - llm_judge

llm_judge:
  enabled: true
  model: "gemini-2.5-pro"  
  // ini betul ga?
  scoring_prompt: |
    You are an expert evaluator. Give a score from 1 to 5 based on:
    - correctness
    - helpfulness
    - completeness
    - reasoning quality
    Respond ONLY with the number.

  api_key_env: "GEMINI_API_KEY"   # ambil dari environment variable (tutor ada di readme yaw)
